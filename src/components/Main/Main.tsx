import React from 'react';
import styles from './main.module.scss';

const Main = () => {
    return (
        <div className={styles.menu}>
            <h1>Привет</h1>
        </div>
    );
};

export default Main;
